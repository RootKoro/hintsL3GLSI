#include<stdio.h> 
#include<string.h>

int main(int argc, char const *argv[])
{
//     int compteur = 0;
//     char phrase[200] ;
//     printf("Entrer votre phrase \n");
//     gets(phrase) ;
//     printf("%s\n",phrase);

// for (int i = 0; i <strlen(phrase); i++)
// {
//     while (phrase[i]==' ' && phrase[i+1]==' ' )
//     {
//         for(int j = i; j < strlen(phrase) - 1; j++)
//             phrase[j] = phrase[j+1];

//         compteur++;
//     }
// }
// printf("%d\n",compteur);
// phrase[strlen(phrase) - compteur] = '\0';
// printf("%s\n",phrase);




    int nombre=12, compteur = 0 , val=0;



// void decompose( int nombre){
    
    int * tab ;

     tab[0] = 15;
    
    printf("%d",tab[0]);
    /* for (int j = 1; j <=nombre; j++)
    {
        int p=0 ;

    for (int i = 1; i <= j; i++)
    {
        if (j%i==0)
        {
           p++; 
        }
        
        
    }
    if (p==2)
    {
        val++ ;
        tab[compteur]=j ;
        compteur++ ;
    }
    
    }
    int taille= (int)sizeof(tab)/sizeof(tab[0]);
    for (int a = 0; a <val ; a++)
    { 
        printf("%d\n ",tab[a]) ;
    
    }
    
    
} */