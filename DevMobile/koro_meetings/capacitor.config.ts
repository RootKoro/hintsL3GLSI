import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'koro_meetings',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
